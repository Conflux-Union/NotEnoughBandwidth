org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Mod properties, shared across all version subprojects; per-version values
# live in versions/<mc>/gradle.properties. Single version for all MC targets;
# common.gradle appends +<mc> (semver build metadata) so the jars stay distinct.
version=1.0.1
group=cn.ussshenzhou
archives_base_name=NotEnoughBandwidth
